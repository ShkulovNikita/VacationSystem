@model EmpRuleViewModel

@using VacationSystem.ViewModels;

@{
    Layout = "_Common";

    ViewData["Title"] = "Информация о правиле для сотрудников";
}

<div class="container">
    <div class="row">
        <div class="col-7">
            <h3>@Model.Rule.Description</h3>
            <h4>Подразделение: @Model.Rule.Department.Name</h4>
            <h5>@Model.Rule.RuleType.Name</h5>
            <a asp-controller="Rules" asp-action="EditEmpRule" asp-route-ruleId="@Model.Rule.Id" class="btn btn-primary">
                Редактировать
            </a>
            <a asp-controller="Rules" asp-action="DeleteEmpRule" asp-route-ruleId="@Model.Rule.Id" class="btn btn-danger">
                Удалить
            </a>
            <h5>Сотрудники в правиле:</h5>
            @foreach(Employee emp in @Model.Employees)
            {
                <card>
                    <p>@emp.LastName @emp.FirstName @emp.MiddleName</p>
                </card>
            }
        </div>
    </div>
</div>