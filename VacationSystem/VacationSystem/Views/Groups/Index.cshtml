@model List<GroupViewModel>

@using VacationSystem.ViewModels;
@using VacationSystem.HtmlHelpers;

@{
    ViewData["Title"] = "Список групп сотрудников";

    string error = null;
    if (TempData["Error"] != null)
        error = TempData["Error"].ToString();

    string message = null;
    if (TempData["Message"] != null)
        message = TempData["Message"].ToString();

    string success = null;
    if (TempData["Success"] != null)
        success = TempData["Success"].ToString();

    List<Department> departments = ViewBag.departments;
}

<div class="container">
    <div class="row">
        <div class="col-10">
            @Html.CheckError(error)
            @Html.CheckMessage(message)
            @Html.CheckSuccess(success)
            <form>
                <label for="query">Подразделение</label>
                <select name="query">
                    @foreach(Department dep in departments)
                    {
                        <option value="@dep.Id">@dep.Name</option>
                    }
                </select>
                <input type="submit" class="btn btn-primary" value="Применить" />
            </form>
            <a asp-controller="Groups" asp-action="AddGroup" class="btn btn-primary">Добавить</a>
            @if(@Model.Count > 0)
            {
                <h3>Группы сотрудников</h3>
            }
            @foreach(GroupViewModel group in @Model)
            {
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            @group.Group.Name
                        </h5>
                        <h6 class="card-subtitle mb-2 text-muted">
                            @group.Department.Name
                        </h6>
                        <p class="card-text">
                            @group.Group.Description
                        </p>
                        <a asp-controller="Groups" asp-action="ViewGroup" asp-route-groupId="@group.Group.Id" class="btn btn-primary">Просмотр</a>
                        <a asp-controller="Groups" asp-action="EditGroup" asp-route-groupId="@group.Group.Id" class="btn btn-primary">Редактировать</a>
                        <a asp-controller="Groups" asp-action="DeleteGroup" asp-route-groupId="@group.Group.Id" class="btn btn-danger">Удалить</a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>